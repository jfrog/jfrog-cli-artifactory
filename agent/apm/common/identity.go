package apmcommon

// PackageManagerID identifies this integration to jfrog-cli-core's shared command plumbing.
const PackageManagerID = "agent-apm"

// CommandNamePrefix is the CommandName() prefix every apm command shares.
const CommandNamePrefix = "rt_agent_apm_"
